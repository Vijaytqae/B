{
  "name": "cypress",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "runpimInchrome": "npx cypress run --spec cypress/e2e/admin/**.cy.js --browser=chrome",
    "runpimInedge": "npx cypress run --spec cypress/e2e/admin/**.cy.js --browser=edge"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cypress": "^13.15.2"
  },
  "devDependencies": {
    "cypress-mochawesome-reporter": "^3.8.2",
    "cypress-xpath": "^2.0.1"
  }
}
